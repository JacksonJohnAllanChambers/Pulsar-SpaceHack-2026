# Ground-side tooling: tests, the GUI console and verifier training. Never installed in the flight image.
-r requirements.txt
pytest>=7.3.0
pytest-cov>=4.1.0
fastapi>=0.110.0
uvicorn>=0.27.0
# training/train_verifier.py only
torch>=2.2.0
onnx>=1.15.0
onnxscript>=0.1.0
# scripts/fetch_sentinel2.py only (windowed reads of public Sentinel-2 COGs)
rasterio>=1.3.0
