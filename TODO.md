# TODO

- Implement ship detection in `process_image`.
- Add automated tests for each workflow stage and queue scheduling.
- Decide whether XML metadata should also be copied to `downlink/coordsDownlink`.
- Handle failed or partial image/XML moves so paired files remain consistent.
- Make polling and downlink settings configurable.